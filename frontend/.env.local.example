# Browser-facing API address for Dayyan's HomeLab.
# This is intentionally the homelab LAN IP, not localhost: browsers on phones,
# tablets, and other PCs need to reach the server over the network.
NEXT_PUBLIC_API_URL=http://10.0.0.223:4000
